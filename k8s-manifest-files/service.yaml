apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  type: Nodeport
  selector:
    app: django 
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      Nodeport: 30007
